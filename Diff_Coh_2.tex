\documentclass[10pt]{amsart}
\usepackage{Diff_Coh}

\title{Differential Cohomology Seminar 2}
\date{06.05.2025 $\&$ 13.05.2025}
\author{Talk by Matthias Frerichs}

\numberwithin{equation}{section}

\begin{document}
\maketitle

In this lecture we want to learn the basics of $\infty$-category theory. For the $\infty$-categorical background, we broadly follow \cite{groth2010inftycats} and a little \cite{lurie2009htt}. This talk assumes familiarity with the language of model categories, see \cite{hirschhorn2003modelcategories}. 

\section{Basics on \texorpdfstring{$(\infty,1)$}{(oo,1)}-categories}

There are a pletora of models for $\infty$-categories, here we focus on two, namely simplicial sets satisfying a filling condition, and categories enriched over simplicial sets.


\subsection{Simplicial sets}
We begin by recalling some definitions about simplicial sets. Denote by $\poset$ the category of finite, linearly ordered sets, and $\BDelta\subseteq\poset$ the full sub-category of sets $[n]=\{0,\cdots,n\}$ (with natural ordering), for every natural number $n$. Given a category $\lC$, a \emph{simplicial object} is a functor $S:\BDelta^\op\to\lC$, the value at $[n]$ is denoted by $S_n$. Simplicial objects in $\lC=\set$ are called \emph{simplicial sets}, the category of simplicial sets is depicted as $\sset$. 
\begin{definition}
  Let $0\leq i\leq n$. Given a simplicial object $S$, Then \begin{enumerate}
    \item The \emph{$i$-face map} $d_i:S_n\to S_{n-1}$ is the one induced by the unique injective, monotone function $f:[n-1]\to[n]$ such that $i$ doesn't belong in the image of $f$. 
    \item The \emph{$i$-degeneracy map} $s_j:S_{n}\to S_{n+1}$ is the one induced by the unique surjective, monotone function $f:[n+1]\to[n]$ such that $f(i)=f(i+1)=i$. 
  \end{enumerate}
\end{definition}
We also use the following special notation: 
\begin{notation}
  0-simplices are called \emph{vertices}, while 1-simplices are called \emph{edges}. The 1-face, resp. 0-face, map $S_1\to S_0$ is called \emph{source}, resp. \emph{target}, and denoted $s$, resp. $t$. Given an edge $f$, we write $f:X\to Y$ to mean $X$ is the source and $Y$ the target. 
\end{notation}
Face and generacy maps (and objects $S_n$, for every $n$) fully determine the simplicial object $S$, see \cite[I.4]{goerssjardine1999simplicialhomotopytheory}. By only depicting face and degeneracy maps, we can represent a simplicial object as follows:
\begin{equation}
\simpset{S_0}{S_1}{S_2}{}{}{}{}  
\end{equation}
\begin{example}
  Given a natural number $n$, denote by $\Delta^n$ the simplicial set represented by $[n]$. In particular, $(\Delta^n)_k$ is the set of monotone functions $[k]\to[n]$. By Yoneda, given a simplicial set $S$, we can (and will) identify the set $S_n$ with $\hom_{\sset}(\Delta^n,S)$. An element $\in S_n$ is called a \emph{$n$-simplex}. 
\end{example}
\begin{example}
  Given a natural number $n$, denote by $\partial\Delta^n\subseteq\Delta^n$ the \emph{boundary} of $\Delta^n$, defined as follows: $f:[k]\to[n]$ in $(\Delta^n)_k$ belongs to $(\partial\Delta^n)_k$ if $f$ is non-surjective. 
\end{example}
\begin{example}
  Given $0\leq i\leq n$, denote by $\Lambda^n_i\subseteq\Delta^n$ the \emph{$i$-horn}, defined as follows: $f:[k]\to[n]$ in $(\Delta^n)_k$ belongs to $(\Lambda^n_i)_k$ if $\{i\}\cup f([k])\neq[n]$. \emph{Right}, resp. \emph{left}, horns are all horns $\Lambda^n_i$ with $0<i\leq n$, resp. $0\leq i<n$. \emph{Inner} horns are all horns $\Lambda^n_i$ with $0<i<n$.  
\end{example}

\subsection{\texorpdfstring{$\infty$}{oo}-Categories} 
\begin{definition}
  Let $\lC$ be a category, $f:X\to Y$ has the \emph{right lifting property} against $i:A\to B$ if for every solid diagram \begin{equation}
    \begin{tikzcd}
      A \arrow[d,"i"']\arrow[r] & X \arrow[d,"f"] \\
      B \arrow[ru,dashed,"\exists" description]\arrow[r] & Y
    \end{tikzcd}
  \end{equation}there is a dashed arrow making the diagram commute. 
\end{definition}
\begin{definition}
  A (\emph{inner}/\emph{right}/\emph{left}) \emph{fibration} is a morphism of simplicial sets $f:X\to Y$ that has the right lifting property against all (inner/right/left) horn inclusions. A simplicial set $X$ is called an \emph{$\infty$-category}, resp. \emph{$\infty$-groupoid}, if the terminal map $X\to\Delta^0$ is an inner fibration, resp. a fibration. 
\end{definition}
\begin{example}
  The nerve of a category, resp. groupoid, is an $\infty$-category, resp. $\infty$-groupoid. 
\end{example}
We recall briefly the construction of the \emph{homotopy category} $\Ho S$ of a $\infty$-category $S$, details can be found in \cite{lurie2009htt,kerodon2026homcat}. The objects of $\Ho S$ are the vertices of $S$. Given a vertex $X$, denote by $\id_X$ the image of $X$ under $s_0:S_0\to S_1$. Given two vertices $X,Y$ and edges $f,g:X\to Y$, we say $f$ is \emph{left homotopic} to $g$ if there is a 2-simplex with the following boundary: 
\begin{equation}
    \begin{tikzcd}
      & Y \arrow[dr,"\id_Y"] & \\
      X \arrow[ru,"f"] \arrow[rr,"g"'] & {} & Y 
    \end{tikzcd}
\end{equation}For $S$ an $\infty$-category, being left homotopic is an equivalence relation on the set of edges $X\to Y$. Denote by $\hom_{\Ho S}(X,Y)$ the set of edges $X\to Y$ modulo being left homotopic. Given an edge $f$, we write $[f]$ for its left homotopy class. Two edges $f:X\to Y$ and $g:Y\to Z$ determine a map $\Phi:\Lambda^2_1\to S$. By definition, there is a 2-simplex $\sigma$ such that $\sigma|_{\Lambda^2_1}=\Phi$, then \begin{equation}
  [g]\circ[f]:=[d_1(\sigma)]
\end{equation}
This composition is well-defined, associative, and unital (with $[\id_X]$ acting as the identity of $X$). 
% -----------TO BE CONTINUED-----------
\begin{definition}
  Let $S$ be a $\infty$-category, an edge $f$ is an \emph{equivalence} if $[f]$ is an isomorphism in $\Ho S$.
\end{definition}
\begin{theorem}[\cite{kerodon2026joyal}]
  A $\infty$-category $S$ is a $\infty$-groupoid if and only if $\Ho S$ is a groupoid. 
\end{theorem}

\subsection{Simplicial categories}

\begin{definition}
  A \emph{simplicial category} is a category enriched over simplicial sets. Denote by $\scat$ the category of simplicial categories. 
\end{definition}

%-----------TO BE CONTINUED-----------

Recall that $\sset$ is self-enriched as follows: 
\begin{definition}
   Given two simplicial sets $X,Y$, the \emph{hom-simplicial set} $\hom^\Delta_{\sset}(X,Y)$ is defined as follows: A $n$-simplex consists of a map of simplicial sets $\Delta^n\times X\to Y$. 
\end{definition}
\begin{lemma}\label{lem:functinfgrpd}
  If $X$ is a $\infty$-groupoid, then $\hom^\Delta_{\sset}(B,X)$ is a $\infty$-groupoid, for every simplicial set $B$. 
\end{lemma}
\begin{proof}
  Corollary of \cite{kerodon2026exponential} by taking $f$ the terminal map $X\to\Delta^0$ and $i$ the initial map $\emptyset\to B$. 
\end{proof}
\begin{definition}
  Let $(P,\leq)$ be a partially ordered set, denote by $\Path(P)$ the following simplicial category: 
  \begin{enumerate}
    \item Objects are the elements of $P$. 
    \item Given $x,y\in P$, let $[x,y]:=\{z\in P|x\leq z\leq y\}$ and $P_{x,y}$ the set of sub-sets $S\subseteq[x,y]$ that are finite, linearly ordered, and contain $\{x,y\}$. We order $P_{x,y}$ by reverse inclusion ($S\leq T$ if $S\supseteq T$). Finally, let $\hom_{\Path(P)}(x,y)$ be the nerve of $(P_{x,y},\supseteq)$. 
    \item Given $x,y,z\in P$, composition \begin{equation}
      \begin{tikzcd}
        \hom_{\Path(P)}(x,y)\times\hom_{\Path(P)}(y,z) \arrow[r] & \hom_{\Path(P)}(x,z)
      \end{tikzcd}
    \end{equation} is the nerve of the union map $\cup:P_{x,y}\times P_{y,z}\to P_{x,z}$. 
  \end{enumerate}
  Given a monotone map $f:P\to Q$, let $\Path(f):\Path(P)\to\Path(Q)$ denote the following functor: 
  \begin{enumerate}
    \item On objects, it is simply $f$.
    \item Given $x,y\in P$, define $\Path(f)$ as the nerve of the functor $f_*:P_{x,y}\to Q_{f(x),f(y)}$ given by taking direct images (i. e. $f_*(S)=f(S)$). 
  \end{enumerate}
Restricting to $\BDelta$, we obtain a cosimplicial simplicial category $\Path:\BDelta\to\scat$. 
\end{definition}
\begin{definition}
  Given a simplicial category $\lC$, its \emph{simplicial nerve} is the simplicial set $\mN_\Delta\lC$ defined as follows:  A $n$-simplex of $\mN_\Delta\lC$ consists of a functor $\Path_n:=\Path(\langle n\rangle)\to\lC$. 
\end{definition}
\begin{remark}$\Path_0$ and $\Path_1$ are the categories $\langle0\rangle$ and $\langle 1\rangle$ viewed as simplicially enriched categories with discrete hom-simplicial sets, therefore a functor $\Path_0\to\lC$, resp. $\Path_1\to\lC$, is determined by a single object, resp. 1-morphism. $\Path_2$ admits the following description:
\begin{enumerate}
    \item Its set of objects is $\langle2\rangle$.
    \item $P_{0,1}$ and $P_{1,2}$ are both singletons, hence $\hom_{\Path_2}(0,1)\cong\hom_{\Path_2}(1,2)\cong\Delta^0$. 
    \item $P_{0,2}=\{[0,2]\leq\{0,2\}\}$ (recall that $P_{i,j}$ is ordered by reversed inclusion), so $\hom_{\Path_2}(0,2)\cong\Delta^1$. Moreover, $[0,2]$ is the composition $\{1,2\}\circ\{0,1\}$.  
  \end{enumerate}
  We can conclude that a functor $\Path_2\to\lC$ is determined by the following data: A triple of 1-morphisms $f:x\to y,g:y\to z$, and $h:x\to y$, together with a 1-simplex $g\circ f\Rightarrow h$ in $\hom_{\lC}(x,z)$. 
\end{remark}
\begin{definition}
  Denote also by $\Path$ the functor $\sset\to\scat$ obtained by left Kan extension of $\Path:\BDelta\to\scat$. 
\end{definition}
\begin{theorem}
  The functor $\Path$ is left adjoint to $\mN_\Delta$ and form a Quillen equivalence between the \emph{Dwyer-Kan-Bergner} model structure on $\scat$ and the \emph{Joyal} model structure on $\sset$. The fibrant objects in the $\sset_\mathrm{Joyal}$, resp. $\scat_\mathrm{DKB}$, are $\infty$-categories, resp. locally fibrant categories. In particular, $\mN_\Delta\lC$ is a $\infty$-category if $\lC$ is locally fibrant.
\end{theorem}
By \cref{lem:functinfgrpd}, we know that the full sub-category $\infty\Grpd\subseteq\sset$ of $\infty$-groupoids is self-enriched, therefore we give the following definition.
\begin{definition}
  Denote by $\lS:=\mN_\Delta(\infty\Grpd)$ the \emph{$\infty$-category of $\infty$-groupoids}.
\end{definition}
\begin{remark}
  In view of \cite{kerodon2026topspc}, we will also use the term \emph{space} to mean $\infty$-groupoid. 
\end{remark}

\section{Basic constructions}

Recall that $\sset$ and $\infty\Grpd$ are self-enriched. Similar to \cref{lem:functinfgrpd}, we have the following: 
\begin{lemma}[\cite{kerodon2026funccat}]
  If $X$ is a $\infty$-category, then $\hom_{\sset}^\Delta(B,X)$ is a $\infty$-category, for every simplicial set $B$. 
\end{lemma}
\begin{definition}
  Given a $\infty$-category $X$ and simplicial set $B$, denote by $\Fun(B,X)$ the \emph{$\infty$-category of functors} $\hom_{\sset}^\Delta(B,X)$. 
\end{definition}
\begin{definition}
  Denote by $\op$ the following endo-functor of $\BDelta$: 
  \begin{enumerate}
    \item On objects,  $\op(\langle n\rangle)=\langle n\rangle$. 
    \item Given $f:\langle k\rangle\to\langle n\rangle$, let $\op(f):\langle k\rangle\to\langle n\rangle$ be the function $\op(f)(i)=n-f(k-i)$. 
  \end{enumerate}Given a simplicial set $X$, denote by $X^\op$ the pre-composition of $X$ with $\op$. 
\end{definition}
\begin{lemma}
  If $X$ is a $\infty$-category, $X^\op$ is also an $\infty$-category. 
\end{lemma}
\begin{definition}
  Given a $\infty$-category $X$, we call $X^\op$ the \emph{opposite $\infty$-category}.  
\end{definition}
\nrnote{Slice $\infty$-categories?}

\section{Accessible and Presentable Categories}

In general, a limit, resp. colimit, preserving functor need not have a left, resp. right, adjoint. Here we wish to introduce a rather large class of $\infty$-categories for which the previous statement holds. We begin by recalling the definition of locally presentable 1-categories. Let $\kappa$ denote a regular cardinal. 
\begin{definition}
  A category $\lI$ is \emph{$\kappa$-filtered} if, for every $\lJ$ with $<\kappa$ many morphisms, every diagram $\lJ\to\lI$ has a cocone. A functor $F:\lC \to \lD$ is \emph{$\kappa$-accessible} if it preserves $\kappa$-filtered colimits. Given a category $\lC$, an object $X$ is \emph{$\kappa$-compact} if $\hom_{\lC}(X,-):\lC\to\set$ is $\kappa$-accessible. 
\end{definition}
\begin{definition}
  A category $\lC$ is \emph{$\kappa$-accessible} if there exists a set $S\subseteq\lC_0$ of $\kappa$-compact objects that generate $\lC$ under $\kappa$-filtered colimits. A category is \emph{accessible} if it is $\kappa$-accessible, for some regular cardinal $\kappa$. 
\end{definition}
\begin{definition}
  A category $\lC$ that is accessible and cocomplete is called \emph{locally presentable}.
\end{definition}
\begin{theorem}
  Let $\lC$ be a category, then $\lC$ is locally presentable if and only if there exists a small category $S$ such that the induced functor $\lC\to\lP(S)$ is a fully faithful, accessible right adjoint. 
\end{theorem}
\begin{theorem}
  Let $\lC,\lD$ be locally presentable categories. A functor $F\colon \lC \to \lD$ is a left, resp. right, adjoint if and only if it preserves colimits, resp. it preserves limits and is accessible.
\end{theorem}
\begin{definition}
  Let $X$ be a $\infty$-category. The \emph{$\infty$-category of pre-sheaves of spaces} is defined as $\lP(X):=\hom_{\sset}(X^{\op},\lS)$.
\end{definition}
\begin{theorem}[Yoneda]
  Given a $\infty$-category $X$, there is a fully faithful functor $y:X\to\lP(X)$, called the \emph{Yoneda embedding}, such that: Given a cocomplete $\infty$-category $Y$, pre-composition by $y$ induces an equivalence \begin{equation}
    \begin{tikzcd}
      \Fun^\mL(\lP(X),Y) \arrow[r] & \Fun(X,Y)
    \end{tikzcd}
  \end{equation}where $\Fun^\mL$ denotes the category of colimit preserving functors. 
\end{theorem} The definition of accessible category transfers directly to the $\infty$-categorical setting. 
\begin{theorem}\label{thm:locpresquasi}
  A $\infty$-category $X$ is locally presentable (cocomplete and accessible) if and only if there is a small sub-$\infty$-category $S$ such that $X\to\Fun(S^\op,\lS)$ is a fully faithful, accessible right adjoint. 
\end{theorem}
\begin{remark}
  In view of \cref{thm:locpresquasi}, one can define a category to be \emph{locally presentable} if it is the accessible right localization of a pre-sheaf category for some small $\infty$-category $S$. In particular, every pre-sheaf category is locally presentable. 
\end{remark}
\begin{theorem}
  Let $X,Y$ be presentable $\infty$-categories, then a functor $f:X\to Y$ is a left, resp. right, adjoint if and only if it preserves colimits, resp. it preserves limits and is accessible.
\end{theorem}
\section{Stable \texorpdfstring{$\infty$}{oo}-Categories and Spectra}
We now use the $\infty$-categorical framework to study spectra. Here are some result that justify the study of spectra. 
\nrnote{Examples of stable phenomena? Freudenthal's suspension theorem, Alexander duality of manifolds, etc.}
\begin{definition}
  Let $\lC$ be an $\infty$-category, an object $0$ that is both initial and terminal is called 
  \emph{zero object}. A category $\lC$ with a zero object is called a \emph{pointed category}.
\end{definition}
\begin{example}
  Let $1\in\lC$ be a terminal object, then the identity of $1$ is the zero object in the slice category $\lC_{1/}$ of objects under $1$. In particular, the category $\lS_*=\lS_{*/}$ of pointed spaces is pointed. 
\end{example}
\begin{proposition}
  Let $\lD$ be a pointed $\infty$-category. Evaluation at the 0-sphere $S^0$ induces an equivalence \begin{equation}
    \begin{tikzcd}
      \Fun^\mL(\lS_*,\lC) \arrow[r] & \lC
    \end{tikzcd}
  \end{equation}
\end{proposition}
We now introduce the notion of a {triangle}.
\begin{definition}
  Let $\lC$ be a pointed $\infty$-category. A \emph{triangle} in $\lC$ is a commutative diagram of the form \begin{equation}\label{eq:triangle}
    \begin{tikzcd}
      X \arrow[r] \arrow[d] & Y \arrow[d] \\
      0 \arrow[r] & Z
    \end{tikzcd}
  \end{equation}A triangle is \emph{exact}, resp. \emph{coexact}, if it is a pullback, resp. pushout, square. 
\end{definition}
\begin{definition}
  Let $\lC$ be a pointed $\infty$-category. Denote by $\lC^{\Sigma}$, resp. $\lC^\Omega$, the full sub-category of $\Fun(\Delta^1\times\Delta^1,\lC)$ of coexact, resp. exact, triangles of the form 
  \begin{equation}\label{eq:exact}
  \begin{tikzcd}
   X \arrow[r]  \arrow[d] & 0 \arrow[d] \\ 
   0 \arrow[r] & Y
  \end{tikzcd},
\end{equation}
\end{definition}
If $\lC$ is finitely cocomplete, resp. complete, for every object $X$, resp. $Y$, there is a contractible space of coexact, resp. exact, triangles as \cref{eq:exact}. In particular, $\lC\simeq\lC^\Sigma$ and $\lC\simeq\lC^\Omega$.  
\begin{proposition}
  If $\lC$ is a finitely complete and cocomplete pointed $\infty$-category, define the following functors Then the functors
  \begin{equation}
    \begin{tikzcd}
      \Sigma:\lC \arrow[r] & \lC^\Sigma \arrow[r,"\mathrm{ev}_{(1,1)}"] & \lC  & & \Omega:\lC \arrow[r] & \lC^\Omega \arrow[r,"\mathrm{ev}_{(0,0)}"] & \lC
    \end{tikzcd}
  \end{equation}are adjoint ($\Sigma$ is left adjoint to $\Omega$). 
\end{proposition}

\begin{theorem}\label{thm:stable}
  Let $\lC$ be a finitely bicomplete pointed $\infty$-category. The following are equivalent:
  \begin{enumerate}
    \item A triangle is exact if and only if it is coexact. 
    \item $(\Sigma,\Omega)$ is an adjoint equivalence.
    \item A commutative square is a pullback if and only if it is a pushout. 
  \end{enumerate}
\end{theorem}
\begin{definition}
  A finite bicomplete pointed $\infty$-category $\lC$ satisfying any of the equivalent conditions in \cref{thm:stable} is called \emph{stable}.
\end{definition}
If $\bA$ denotes a nice abelian category, there is a \emph{derived $\infty$-category} of $\bA$, denoted $\lD(\bA)$ such that $\Ho\lD(\bA)=D(\bA)$ is the ordinary derived category of $\bA$, i.e. the localization of chain complex at quasi-isomorphisms. From homological algebra, it is known that $D(\bA)$ underlies the structure of a triangulated category, which turns out to be the 1-categorical reflection of $\lD(\bA)$ being stable. 
\begin{proposition}[{\cite[3.11]{lurie2017ha}}]
  If $\lC$ is a stable $\infty$-category, then $\Ho\lC$ has a canonical structure of a triangulated category. 
\end{proposition}To construct the stabilization of a pointed $\infty$-category, there are several approaches, such as reduced excisive functors on $\lS^\mathrm{fin}_*$, the category of pointed, finite spaces, see \cite[1.4.2.8]{lurie2017ha}. Here we consider the more explicit approach using spectrum objects.
\begin{definition}
  Let $\lC$ be a pointed $\infty$-category. A \emph{pre-spectrum object in $C$} consists of a functor $E:\bZ\times\bZ\to\lC$ such that $E(n,m)\simeq0$, for all $n\neq m$. Denote by $\PSp(\lC)$ the category of pre-spectrum objects. The functor $\Omega^{\infty-n}:\PSp(\lC)\to\lC$ is defined as evaluation at $(n,n)$. 
\end{definition}
For every $n$, the diagram \begin{equation}
  \begin{tikzcd}
    E(n,n) \arrow[r]\arrow[d] & 0 \arrow[d] \\
    0 \arrow[r] & E(n+1,n+1) 
  \end{tikzcd}
\end{equation}determines a pair of adjoint morphisms \[\alpha_n:\Sigma E(n,n)\to E(n+1,n+1),\qquad\beta_n:E(n,n)\to\Omega E(n+1,n+1)\]
\begin{definition}
  Let $\lC$ be a pointed $\infty$-category. A \emph{spectrum object in $\lC$} consists of a pre-spectrum object $E$ such that $\beta_n$ is an equivalence, for all $n$. Denote by $\Sp(\lC)\subseteq\PSp(\lC)$ the full sub-category of spectrum objects. 
\end{definition}
\begin{theorem}
  Let $\lC$ be a presentable pointed $\infty$-category, then $\Omega^{\infty-n}:\Sp(\lC)\to\lC$ admits a left adjoint $\Sigma^{\infty-n}:\lC\to\Sp(\lC)$, for every $n$. 
\end{theorem}
In particular, $\Sigma^\infty:\lC\to\Sp(\lC)$ has the following universal property. 
\begin{theorem}
 Let $\lC$ be a presentable pointed $\infty$-category. Given a stable $\infty$-category $\lD$, pre-composition by $\Sigma^\infty$ induces an equivalence 
 \begin{equation}
  \begin{tikzcd}
    \Fun^\mL(\Sp(\lC),\lD) \arrow[r] & \Fun^\mL(\lC,\lD)
  \end{tikzcd}
 \end{equation}In particular, for $\lC=\lS_*$, evaluation at the \emph{sphere spectrum} $\bS=\Sigma^\infty S^0$ induces an equivalence
 \begin{equation}
  \begin{tikzcd}
    \Fun^\mL(\Sp(\lS_*),\lD) \arrow[r] & \lD
  \end{tikzcd}
 \end{equation}
\end{theorem}
\begin{definition}
  The \emph{$\infty$-category of spectra} is the category of spectrum objects in pointed spaces. 
\end{definition}

\section{Generalized Cohomology Theories}

We shall now use the language of $\infty$-categories to reformulate the concept of generalized cohomology theory \` a-l\`a Eilenberg-Steenrod. In this new context, we recall a representability theorem for cohomology theories by spectrum object. 
\begin{remark}
  Denote by $\set^\bZ$ the category of $\bZ$-indexes families of sets. Given an object $S$ and $n\in\bZ$, denote by $\Sigma^n S$ the shifted family $(\Sigma^nS)_i=S_{i-n}$.  
\end{remark}
\begin{definition}[{\cite[1.4.1.6]{lurie2017ha}}]\label{def:cohthe}
  Let $\lC$ be a finitely cocomplete pointed $\infty$-category, $\Sigma_\lC:\lC \to \lC$ the induced suspension functor. A \emph{generalized cohomology theory} is a functor $H:\Ho\lC^{\op}\to\set^\bZ$ together with a natural isomorphism $\partial:\Sigma H\to H\Sigma_\lC$ such that:
  \begin{itemize}
    \item $H$ preserves arbitrary products. In particular, $H^n(0)$ is the one-point set. Given an object $X$, the unique morphism $X\to0$ induces an element $*\simeq H^n(0)\to H^n(X)$, which we denote by $0$. 
    \item Given a coexact triangle $X'\to X\to X''$, if $\eta\in H^n(X)$ has image $0\in H^n(X'')$, then it lies in the image of $H^n(X')\to H^n(X)$. 
  \end{itemize}
\end{definition}
\begin{theorem}[{\cite[1.4.1.10]{lurie2017ha}}]
 Let $\lC$ be a nice $\infty$-category and $(H,\partial)$ a generalized cohomology theory, then, for every $n$, the functor $H^n$ is representable by an object $E(n)$. 
\end{theorem}
The natural isomorphism $\partial$ translates into an equivalence $E(n)\simeq\Omega E(n+1)$, which is then used to construct a spectrum object representing the cohomology theory $H^n$, see \cite[1.4.1.11]{lurie2017ha}.
\begin{remark}
  For $\lC=\lS_*$, the above definition of cohomology theory reduces to the classical Eilenberg-Steenrod definition. Since $\lS_*$ is nice, we thus recover the classical \emph{Brown representability theorem}. 
\end{remark}

{\footnotesize
\bibliographystyle{alpha}
\bibliography{main}
}
\end{document}